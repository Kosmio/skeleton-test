# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## Project Overview

**Skeleton Test** is a full-stack project combining a Strapi v5 headless CMS with an Astro 6 SSR frontend, with content management, contact forms, newsletter subscriptions, and Docker-based deployment.

**Key Technologies:**
- **Backend**: Strapi v5.40 (Node.js headless CMS, REST API, PostgreSQL)
- **Frontend**: Astro 6 SSR with Node.js adapter, React 19 for interactive components
- **Styling**: Tailwind CSS v4 (CSS-based config via `@tailwindcss/vite`)
- **Reverse Proxy**: Traefik v3 with Let's Encrypt TLS
- **CI/CD**: GitHub Actions (build/push images + SSH remote deployment)
- **Package Manager**: pnpm
- **Infrastructure**: Docker Compose with base + overlay pattern, multi-stage Dockerfiles

## Project Structure

```
skeleton-test/
├── .github/workflows/              # CI/CD
│   ├── dev.yml                     # Build & push Docker images on push to main
│   └── deploy.yml                  # Manual SSH remote deployment
├── strapi/                         # Strapi v5 backend
│   ├── config/                     # Environment-driven config (server, db, admin, api)
│   ├── src/
│   │   ├── admin/app.js            # Admin panel config (locale)
│   │   ├── index.js                # Bootstrap (locale setup, seed data)
│   │   └── api/                    # Content types + custom controllers
│   │       ├── article/            # Articles (standard CRUD via factories)
│   │       ├── contact/            # Contact form (custom POST endpoint)
│   │       └── newsletter/         # Newsletter subscription (Altcha + Brevo)
│   ├── mcp/                        # MCP server for AI-assisted content management
│   │   ├── index.js                # Server entry point (stdio transport, 10 tools)
│   │   ├── lib/strapi-client.js    # HTTP client for Strapi REST API
│   │   ├── content-types.json      # Content type definitions (maintenance contract)
│   │   ├── README.md               # Setup guide for clients (multi-tool)
│   │   └── SETUP_PROMPT.md         # AI-agent-readable setup instructions
│   └── .env.example
├── web/                            # Astro 6 SSR frontend
│   ├── astro.config.mjs            # SSR + React + Tailwind + Sitemap
│   ├── src/
│   │   ├── styles/app.css          # Tailwind v4 theme
│   │   ├── layouts/Layout.astro
│   │   ├── components/             # Astro components (Header, Footer, Hero, Card, Button)
│   │   ├── react-components/       # React components (ContactForm, ArticleList, MarkdownContent)
│   │   ├── pages/                  # File-based routing (index, articles, contact, 404)
│   │   └── lib/                    # API client (strapi.ts) + types (types.ts)
│   └── public/                     # Static assets (favicon, cookie consent)
├── infra/
│   ├── deploy/                     # Docker Compose base + overlays (local/dev/prod)
│   │   └── scripts/deploy.sh       # Unified deployment script
│   ├── docker/                     # Dockerfiles (strapi, website, mcp)
│   └── make/                       # Per-service Makefiles
├── reports/quality/                 # Quality audit reports (generated by /quality)
├── .claude/skills/
│   ├── quality/                     # Unified quality audit (orchestrates a11y, SEO, eco, GDPR)
│   │   └── SKILL.md
│   ├── accessibility/               # WCAG 2.1 AA audit (axe-core + jsx-a11y)
│   │   ├── SKILL.md
│   │   └── a11y-audit/              # Bundled snapsynapse/skill-a11y-audit
│   ├── seo/                         # SEO audit (Lighthouse + 20 bundled sub-skills)
│   │   ├── SKILL.md
│   │   └── seo-skills/              # Bundled aaron-he-zhu/seo-geo-claude-skills
│   ├── ecoconception/               # Eco-design audit (EcoIndex + RGESN)
│   │   └── SKILL.md
│   ├── gdpr/                        # GDPR/RGPD compliance audit
│   │   └── SKILL.md
│   ├── security/                    # Web security audit (OWASP + Astro/Strapi-specific)
│   │   └── SKILL.md
│   ├── setup-deploy/               # Deployment infrastructure setup wizard
│   │   └── SKILL.md
│   ├── deploy/                      # Deploy to dev/prod via GitHub Actions
│   │   └── SKILL.md
│   └── start/                       # Project initialization wizard
│       └── SKILL.md
├── Makefile                        # Root orchestration
├── VERSION                         # Project version (used in Docker tags)
└── ai/                             # Practice documentation
    ├── STRAPI_PRACTICES.md
    ├── ASTRO_PRACTICES.md
    └── INFRA_PRACTICES.md
```

## Quick Start

```bash
# Start Postgres
make infra-up

# Start Strapi (terminal 1)
cd strapi && pnpm develop
# → http://localhost:1337/admin

# Start web (terminal 2)
cd web && pnpm dev
# → http://localhost:4321
```

On first Strapi start, the bootstrap seeds 3 demo articles and sets French as the default locale.

## Key Architecture Decisions

### Strapi v5
- API responses are flat: `article.title`, `article.image?.url`
- Use `strapi.documents()` (Document Service) for all data operations
- i18n is a core plugin, no separate dependency needed
- Admin panel language configured in `src/admin/app.js` (default: French)
- Admin URL is configurable via `ADMIN_URL` env var (default: `/admin`)

### Astro SSR
- All pages are server-rendered at request time (`output: "server"`)
- Dynamic routes fetch data on each request
- React components hydrate client-side via `client:visible`
- Sitemap auto-generated via `@astrojs/sitemap` (requires `site` in config)

### Tailwind v4
- Theme defined in CSS (`src/styles/app.css` with `@theme` block)
- Uses `@tailwindcss/vite` plugin in Astro's Vite config

### Reverse Proxy (Traefik)
- Traefik v3 handles TLS termination with Let's Encrypt ACME
- Single-domain routing: web serves `/`, Strapi serves `/strapi`, MCP serves `/mcp` — all via path prefix + strip middleware
- Disabled in local development (Strapi and web run on the host)
- Configured via Docker labels in the environment overlays

### CI/CD (GitHub Actions)
- `dev.yml`: Builds and pushes Docker images to GHCR on push to main or tags
- `deploy.yml`: Manual workflow for SSH remote deployment with environment selection, version input, optional DB reset
- Secrets managed via GitHub repository secrets, merged into `.env.secrets` at deploy time

### Content Management MCP
- The MCP server (`strapi/mcp/`) runs as a Docker service alongside Strapi and is exposed via Traefik at `/mcp`
- Clients connect their AI tool (Claude, ChatGPT, Gemini, Cursor, etc.) by pointing it at `https://{domain}/mcp` — no local installation needed
- Supports both Streamable HTTP (current MCP spec) and legacy SSE transport for broad tool compatibility
- Authentication uses Strapi's built-in API tokens: clients create a token in Strapi admin and pass it via the `Authorization` header in their MCP config — no server-side secrets needed
- Uses a static config file (`strapi/mcp/content-types.json`) that describes available content types and their fields
- **Maintenance contract**: `content-types.json` MUST be kept in sync with actual Strapi content types. When content types are added, modified, or removed, update this file to match
- AI agents discover the MCP via `/.well-known/mcp.json` (structured), `/llms.txt` (summary), and `/llms-full.txt` (full setup guide) — all served as static files from the Astro frontend
- `strapi/mcp/SETUP_PROMPT.md` is the internal setup guide for agents working within the project
- The MCP server is part of the skeleton and must be maintained with the same care as any production code

### Environment variable split
- `STRAPI_URL` + `STRAPI_KEY` — server-side only (in Astro frontmatter)
- `REACT_STRAPI_URL` — public URL passed as props to React components
- `PUBLIC_*` — available everywhere (Matomo)
- `HOST_NAME` — domain for Traefik routing
- `ACME_EMAIL` — Let's Encrypt certificate notifications
- `ADMIN_URL` — configurable Strapi admin path

## Build & Deploy

```bash
# Build Docker images
make build

# Lint Dockerfiles
make lint

# Push to registry
make stage

# Deploy to environment (local)
./infra/deploy/scripts/deploy.sh <local|dev|prod> up

# Deploy via GitHub Actions (remote)
# Use the "Deploy" workflow in GitHub Actions UI
```

## Deployment Status

`/setup-deploy` was run on 2026-04-10. Deployment infrastructure is configured:
- GitHub secrets: configured (Strapi secrets, Postgres password, Altcha HMAC key) — **SSH key NOT set** (ask Luc)
- GitHub variables: configured (DEPLOY_HOST=test.kosmio.dev, DEPLOY_USER=kosmio, SSH_PORT=2234)
- GitHub environments: dev, prod
- Local env files: strapi/.env, web/.env
- Dev domain: test.kosmio.dev
- Prod domain: not configured yet

## Quality Audits

- `/quality` — **Start here.** Interactive quality audit across 5 categories. Saves reports to `reports/quality/`.
- `/accessibility` — Standalone WCAG 2.1 AA audit (axe-core + jsx-a11y)
- `/seo` — Standalone SEO audit (Lighthouse + meta/OG/structured data)
- `/ecoconception` — Standalone eco-design audit (EcoIndex + RGESN)
- `/gdpr` — Standalone GDPR compliance audit
- `/security` — Standalone web security audit (OWASP, headers, deps, input validation)

All audits require the dev server running (`cd web && pnpm dev`).

## Deployment

- `/setup-deploy` — Interactive wizard to configure deployment: generates secrets, sets up GitHub secrets/variables/environments, creates local `.env` files. Run once before first deployment.
- `/deploy` — Deploy to dev or prod. Runs pre-flight checks, triggers the GitHub Actions pipeline, monitors progress. Designed for non-technical users.

## Practices

Read `.claude/practices.md` for the index of all practice docs. Key docs:
- `ai/STRAPI_PRACTICES.md` — Strapi content types, controllers, config, bootstrap, MCP maintenance
- `ai/ASTRO_PRACTICES.md` — Pages, components, hydration, API client, Tailwind v4
- `ai/INFRA_PRACTICES.md` — Docker, deployment, Makefiles, environment management
