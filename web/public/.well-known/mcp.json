{
  "mcp": {
    "servers": [
      {
        "name": "skeleton-cms",
        "description": "Manage Skeleton website content (articles, media) via MCP. CRITICAL: You MUST read /llms.txt BEFORE attempting any setup or connection. It contains required authentication steps without which the MCP will reject all requests.",
        "transport": {
          "type": "http",
          "url": "/mcp"
        }
      }
    ]
  },
  "documentation": {
    "setup_guide": "/llms.txt"
  }
}
