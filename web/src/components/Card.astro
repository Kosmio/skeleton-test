---
export interface Props {
  image?: string;
  title: string;
  text?: string;
  slug: string;
  date?: string;
}

const { image, title, text, slug, date } = Astro.props;
---

<a
  href={`/articles/${slug}`}
  class="group flex flex-col bg-white rounded-xl overflow-hidden border border-gray-100 hover:border-gray-200 hover:shadow-lg transition-all duration-300"
>
  {image ? (
    <div class="aspect-[16/10] overflow-hidden bg-surface">
      <img
        src={image}
        alt={title}
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
      />
    </div>
  ) : (
    <div class="aspect-[16/10] bg-gradient-to-br from-surface to-surface-alt flex items-center justify-center">
      <svg class="w-12 h-12 text-gray-300" aria-hidden="true" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
      </svg>
    </div>
  )}
  <div class="p-5 flex flex-col flex-grow">
    {date && (
      <time class="text-xs text-muted mb-2">{date}</time>
    )}
    <h3 class="text-base font-semibold text-gray-900 mb-2 line-clamp-2 group-hover:text-primary transition-colors">
      {title}
    </h3>
    {text && (
      <p class="text-sm text-muted line-clamp-3 flex-grow">{text}</p>
    )}
    <span class="mt-4 inline-flex items-center gap-1 text-sm font-medium text-primary">
      Lire l'article
      <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" aria-hidden="true" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
      </svg>
    </span>
  </div>
</a>
